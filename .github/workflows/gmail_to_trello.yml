# …
jobs:
  gmail-to-trello:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PYTHONUNBUFFERED: "1"

      # Gmail IMAP
      IMAP_USER:  ${{ secrets.IMAP_USER }}
      IMAP_PASS:  ${{ secrets.IMAP_PASS }}

      # Trello
      TRELLO_KEY:         ${{ secrets.TRELLO_KEY }}
      TRELLO_TOKEN:       ${{ secrets.TRELLO_TOKEN }}
      TRELLO_BOARD_ID:    ${{ secrets.TRELLO_BOARD_ID }}
      TRELLO_DEST_LIST_ID:${{ secrets.TRELLO_DEST_LIST_ID }}

      # ---------- R2 (needed for delete markers) ----------
      R2_ACCOUNT_ID:        ${{ secrets.R2_ACCOUNT_ID }}
      R2_ACCESS_KEY_ID:     ${{ secrets.R2_ACCESS_KEY_ID }}
      R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
      # Either of these names is fine; the script reads both:
      R2_BUCKET_NAME:       ${{ secrets.R2_BUCKET_NAME }}
      # R2_BUCKET:          ${{ secrets.R2_BUCKET }}   # (optional alternative)
      R2_MARKER_PREFIX:     delete_markers
      R2_DELETE_AFTER_DAYS: "30"

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          python -m pip install requests boto3

      - name: Run sync
        run: python gmail_to_trello_reply_sync.py
